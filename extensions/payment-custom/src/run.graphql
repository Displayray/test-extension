query RunInput {
  paymentCustomization {
    metafield(namespace: "$app:payment-custom", key: "function-configuration") {
      value
    }
  }
  cart {
    cost {
      totalAmount {
        amount
      }
    }
  }
  paymentMethods {
    id
    name
  }
}
